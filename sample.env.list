#
# from https://hub.docker.com/r/zaporylie/drupal/
# These are default ENV values.
# copy this file to "env.list" in the same directory:
#   cp sample.env.list env.list
# and edit env.list to adapt as required.
#
NGINX_DRUPAL_ROOTDIR=/var/www/site/drupal7
DRUPAL_REPO_ROOTDIR=/var/www/site
DRUPAL_DB=drupal
DRUPAL_DB_USER=drupal
DRUPAL_DB_PASSWORD=drupal
DRUPAL_PROFILE=minimal
DRUPAL_SUBDIR=default
DRUPAL_MAJOR_VERSION=7
DRUPAL_DOWNLOAD_METHOD=drush  # drush | git
DRUPAL_GIT_BRANCH=7.x    # Only if DRUPAL_DOWNLOAD_METHOD is git
DRUPAL_GIT_DEPTH=1    # Only if DRUPAL_DOWNLOAD_METHOD is git
METHOD=auto    # Synchronization method (use drush sql-sync or file)
MYSQL_HOST_NAME=mysql    # skip this if you're not linking mysql container
DRUPAL_TEST=0
BUILD_TEST=0
